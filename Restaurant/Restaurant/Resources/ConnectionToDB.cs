using Microsoft.Data.Sqlite;
using System;
using System.Collections.Generic;
using System.Data;
using System.Data.SQLite;
using System.IO;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Restaurant.Resources
{
    class ConnectionToDB
    {
        /*SQLiteConnection con;
        SQLiteCommand cmd;
        SQLiteDataReader dr;

        public void CreateDatabaseAndTable()
        {
           con = new SQLiteConnection("Data Source=MenuInfo.db;Version=3");
        }

        public void SelectData()
        {
            int counter = 0;
            cmd = new SQLiteCommand("Select Name From ItemDetailes", con);
            con.Open();
            dr = cmd.ExecuteReader();
            while (dr.Read())
            {
                counter++;
                Console.WriteLine(dr[0] + " : " + dr[1] + " " + dr[2]);

            }
            con.Close();

        }*/


/*        private const string DatabaseFile = "MenuInfo.db";
        private const string DatabaseSource = "data source=" + DatabaseFile;
*/
    }

}
